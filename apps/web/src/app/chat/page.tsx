/** `/chat` — the chat shell itself lives in this segment's layout (`ChatLayout`), which
 *  swaps conversations via history.pushState so ChatView never remounts. */
export default function ChatPage() {
  return null;
}
