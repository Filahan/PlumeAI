/** Moved to `components/tools/tools-view.tsx` when the page grew an MCP section.
 *  Re-exported here so `app/tools/page.tsx` keeps its import path. */
export { default } from '@/components/tools/tools-view';
